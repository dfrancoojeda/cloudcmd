trigger:
- main

pool:
  vmImage: 'ubuntu-latest'

steps:
- task: UseNode@2
  inputs:
    versionSpec: '14.x'
  displayName: 'Instalar Node.js'

- script: |
    npm install -g cloudcmd
  displayName: 'Instalar Cloudcmd'

- script: |
    cloudcmd
  displayName: 'Ejecutar Cloudcmd'
